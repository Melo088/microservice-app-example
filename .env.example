JWT_SECRET=myfancysecret

# Opcionales. Si se dejan vacías, el filtro notify de log-message-processor
# solo registra el error por log en vez de enviar un correo con SendGrid.
SENDGRID_API_KEY=
SENDGRID_TO=