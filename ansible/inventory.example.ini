; Copiar a inventory.ini (ignorado por git) y completar con la IP que entrega
; "terraform output vm_public_ip" despues de un "terraform apply" en ../terraform.

[app_servers]
app ansible_host=REEMPLAZAR_CON_LA_IP_PUBLICA ansible_user=azureuser ansible_ssh_private_key_file=~/.ssh/microservice-app-example-azure
